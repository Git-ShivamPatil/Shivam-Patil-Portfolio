# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files (never commit real secrets)
.env
.env*.local
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma generated client (regenerated via `pnpm db:generate`)
/lib/generated/prisma

# editor
.vscode/*
!.vscode/extensions.json
.idea

# Claude Code local session state (not project config)
.claude/settings.local.json

# superseded local files, not part of the deployed site
/ignore_old
.env*

# The root copy of logo.jpeg was a stray duplicate of public/logo.jpeg
# (byte-identical). The one under public/ is the one next/image serves.
logo.jpeg

# Playwright
playwright-report/
test-results/
.lighthouseci/
